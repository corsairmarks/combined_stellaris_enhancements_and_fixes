living_standard_organic_trophy = {
	token = living_standard_organic_trophy
	name = living_standard_organic_trophy

	triggered_pop_modifier = {
		potential = {
			NAND = {
				exists = owner
				owner = { has_deficit = consumer_goods }
			}
		}
		pop_happiness = 0.4
	}

	potential = {
		host_has_dlc = "Synthetic Dawn Story Pack"
		exists = from
		from = {
			has_authority = auth_machine_intelligence
			has_valid_civic = civic_machine_servitor
		}
	}

	allow = {
		hidden_trigger = { exists = from }
		custom_tooltip = {
			fail_text = species_is_not_trophy
			OR = {
				has_citizenship_type = {
					type = citizenship_organic_trophy
					country = from
				}
				# TODO: in order to not need to edit the bio_trophy job, apply more edits to game rules
				# commit the working code first, however
				AND = {
					has_species_flag = allow_leaders_organic_advisors@from
					has_citizenship_type = {
						type = citizenship_full
						country = from
					}
				}
			}
		}
	}

	ai_will_do = {
		factor = 100
	}
}